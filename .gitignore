# dependencies
node_modules/

# yarn (berry) — keep config + lockfile, ignore caches/state
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
.pnp.*

# the generated worked example under test/ is committed on purpose (probe.json,
# state-machine.json, test-plan.md, tests.spec.ts) so the output is reviewable.
test/integration/__playwright/pr-comment.md

# playwright run output
playwright-report/
test-results/

# build / misc
dist/
*.log
.DS_Store
.env
.env.local
